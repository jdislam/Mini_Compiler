MOV f, 1.7
Initialize variable f with the float 1.7.

MOV g, 0.5
Initialize variable g with 0.5.

;; function main
Comment; ignored by the VM.

main:
Label marking the start of the function body.

JNZ t1, Lt2
Jump to Lt2 if t1 is non-zero.
⚠️ Problem: t1 hasn’t been defined yet. You normally set t1 with a compare, e.g. GE t1, f, g or GT t1, f, g.

JMP Lt3
If t1 == 0, go to the else block at Lt3.

Lt2:
Then-block label.

ADD, t5, g, f
Compute t5 = g + f.
(The extra comma after ADD is a cosmetic quirk; your VM tolerates it, but the canonical form is ADD t5, g, f.)

JMP Lt4
Skip the else block and go to the join.

Lt3:
Else-block label.

t6 = - f
Unary negation; t6 = -f.

SUB, t7, g, t6
Compute t7 = g - t6. Since t6 = -f, this is g - (-f) = g + f.
⚠️ If you meant “else: g - f”, you should do SUB t7, g, f (no negation temporary needed).

Lt4:
Join point after then/else.

MOV RET, 0
Set the function’s return value to 0.

RET
Return from main.